{
  "name": "ezrpc",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "workspaces": [
    "packages/*",
    "demo/*"
  ],
  "scripts": {
    "build:common": "yarn workspace @ezrpc/common build",
    "build:server": "yarn workspace @ezrpc/server build",
    "build:client": "yarn workspace @ezrpc/client build",
    "build": "yarn build:common && yarn build:server && yarn build:client",
    "sample:server:start": "yarn workspace sample-server start",
    "sample:client:start": "yarn workspace sample-client start"
  },
  "devDependencies": {
    "@types/node": "^15.6.1",
    "tsc-watch": "^4.4.0",
    "typescript": "^4.3.2"
  },
  "dependencies": {
    "rimraf": "^3.0.2"
  }
}
